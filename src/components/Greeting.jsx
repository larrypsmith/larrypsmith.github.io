import React from 'react';
import {
  Paper,
  Typography
} from '@material-ui/core';

export default () => (
    <Paper elevation={0}>
      <Typography variant="h3">
        Hi, I'm Larry. I build web apps.
      </Typography>
    </Paper>
)